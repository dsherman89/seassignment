# seassignment

Currently hold all backend code tested with Postman
Node_modules has purposely been left off due to size and with the package.json these will install on build.


This can be ran locally with node.

Go to terminal, use command 'node app.js' when in the folder of the downloaded content.
Then go to a web browser and go to 'http://localhost:3000/'
